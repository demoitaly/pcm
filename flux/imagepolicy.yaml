apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: demo-image-policy
  namespace: progetto-alfa-tvbrq
spec:
  imageRepositoryRef:
    name: demo-image-policy
  policy:
    semver:
      # Prende l'ultima versione stabile disponibile"
      range: ">=1.0.0" 
