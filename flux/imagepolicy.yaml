apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: demo
spec:
  imageRepositoryRef:
    name: demo
  filterTags:
    pattern: 'v(?P<version>[0-9]+)$'
    extract: '$version'
  policy:
    alphabetical:
      order: asc
